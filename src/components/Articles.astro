---

// let allArticles = Astro.fetchContent('./pages/articles/*.astro');
// let allArticles = await Astro.glob('./articles/*.astro');
// allArticles = allArticles.sort((a, b) => new Date(b.date).valueOf() - new Date(a.date).valueOf());

// consqt articles = allArticles.filter(article => article.date);

---